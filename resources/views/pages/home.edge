@layout()
  @let(url = request.url())
  @let(urls = {
    home: '/',
  })
  <header class="header relative min-h-140">
    <nav class="container flex mx-auto items-center mt-8" x-data="burger">
      <div
        class="text-white text-4xl uppercase mr-6 border-y-2 py-2 border-amber-500 tracking-wide"
      >
        Loca<span class="text-amber-500">van</span>
      </div>
      <ul class="container mx-auto flex space-x-4 items-center text-white p-4">
        <li class="hidden sm:block">
          <a
            class="tracking-wide linkanimate {{ urls.home ? 'active font-bold' : '' }}"
            href="{{ urlFor('home.index') }}"
          >
            Accueil
          </a>
        </li>
        <li class="hidden sm:block">
          <a href="#" class="tracking-wide linkanimate">Nos véhicules</a>
        </li>
        <li class="hidden sm:block">
          <a href="#" class="tracking-wide linkanimate">Devenir loueur</a>
        </li>
        <li class="hidden sm:block"><a href="#" class="tracking-wide linkanimate">Aide</a></li>
        @if(!auth.isAuthenticated)
          <li class="hidden sm:block ml-auto">
            <a class="tracking-wide linkanimate font-bold" href="{{ urlFor('session.create') }}">
              Connexion
            </a>
          </li>
          <li>
            <a
              href="{{ urlFor('new_account.create') }}"
              class="bg-amber-500 text-amber-950 p-3 rounded-lg font-bold hover:bg-amber-500/80 transition-colors"
            >
              Inscription
            </a>
          </li>
        @else
          <li class="hidden sm:block">
            @link({
              route: 'vehicles.create',
              class: 'tracking-wide linkanimate',
            })
              Déposer une annonce
            @end
          </li>
          <div x-data="dropdown" class="relative">
            <button
              id="dropdownDefault"
              class="group text-white hover:bg-neutral-500 font-medium rounded-lg text-sm px-4 py-2.5 text-center flex items-center gap-3 cursor-pointer"
              type="button"
              @click="toggle()"
            >
              {{ auth.user.firstName }}
              {{ auth.user.lastName }}
              <img
                class="w-10 h-10 rounded-full"
                src="{{ await driveUrl(auth.user.avatarPath) }}"
                alt="avatar image"
              />
              <i class="fa-solid" :class="dropped ? 'fa-chevron-up' : 'fa-chevron-down'"></i>
              <!--Dropdown menu-->
            </button>
            <!--Dropdown-->
            <div
              x-show="dropped"
              id="dropdown"
              class="z-10 bg-white rounded shadow w-54 absolute mt-3 overflow-hidden"
            >
              <ul class="text-sm text-gray-700 divide-y divide-gray-200">
                <li>
                  <a href="{{ urlFor('profile') }}" class="dropdown-item">
                    <i class="fa-solid fa-user w-6"></i>Modifier profil
                  </a>
                </li>
                <li>
                  <a href="{{ urlFor('bookings.bookings') }}" class="dropdown-item">
                    <i class="fa-solid fa-caravan w-6"></i>Mes réservations
                  </a>
                </li>
                <li>
                  <a href="{{ urlFor('vehicles.listing') }}" class="dropdown-item">
                    <i class="fa-solid fa-book-open w-6"></i>Mes annonces
                  </a>
                </li>
                @if(auth.user.isAdmin)
                  <li>
                    <a href="{{ urlFor('admin.index') }}" class="dropdown-item">
                      <i class="fa-solid fa-user-key"></i>Admin
                    </a>
                  </li>
                @end

                <li>
                  @form({
                    route: 'session.destroy',
                    method: 'POST',
                  })
                    @button({
                      class:
                        'flex items-center w-full dropdown-item cursor-pointer bg-gray-200 hover:bg-gray-300',
                    })
                      <i class="fa-solid fa-arrow-right-from-bracket w-6"></i>
                      <span>Déconnexion</span>
                    @end
                  @end
                </li>
              </ul>
            </div>
          </div>
        @end

        <li class="sm:hidden cursor-pointer" @click="toggle()">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-8 w-8 stroke-amber-500"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="2"
          >
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </li>
      </ul>
      {{-- Mobile menu --}}
      @!burger()
    </nav>

    <div class="container mx-auto mt-13.5">
      <div class="max-w-180">
        <span class="py-1.5 px-3 bg-amber-500 rounded-full font-poppins text-[13px] font-medium">
          128 vans disponibles ce week-end
        </span>
        <h1 class="font-bold text-white text-[52px] tracking-wide mt-4.5 leading-[1.10]">
          Louez le van d'un particulier, près de chez vous
        </h1>
        <p class="mt-2 text-white/90 text-[18px] max-w-[52ch]">
          Vans, fourgons aménagés et intégraux à partir de 58 € la nuit. Assurance et assistance incluses, annulation gratuite jusqu'à 48 h.
        </p>
        {{--
          Seule "Où" filtre réellement pour l'instant (route vehicles.search) ;
          Dates et Voyageurs sont posés visuellement en préparation de F4
          (recherche par disponibilité)
        --}}
        @form({
          route: 'vehicles.search',
          method: 'GET',
          class:
            'flex bg-white mt-[24px] rounded-[14px] justify-between divide-x divide-gray-200',
        })
          <div class="px-4.5 py-3 flex flex-col w-1/4">
            @field.root({ name: 'city' })
              @!field.label({
                text: 'Où',
                class: 'text-[#666D7D] text-[11px] font-semibold uppercase',
              })
              @!input.control({
                type: 'text',
                placeholder: 'Ville ou région',
                class: 'mt-[2px] focus:outline-none text-[15px]',
              })
            @end
          </div>
          <div class="px-4.5 py-3 flex flex-col w-1/4">
            @field.root({ name: 'date' })
              @!field.label({
                text: 'Dates',
                class: 'text-[#666D7D] text-[11px] font-semibold uppercase',
              })
              @!input.control({
                type: 'text',
                placeholder: 'Départ - retour',
                class: 'mt-[2px] focus:outline-none text-[15px]',
              })
            @end
          </div>
          <div class="px-4.5 py-3 flex flex-col w-1/4">
            @field.root({ name: 'voyageur' })
              @!field.label({
                text: 'Voyageurs',
                class: 'text-[#666D7D] text-[11px] font-semibold uppercase',
              })
              @!input.control({
                type: 'text',
                placeholder: '2 voyageurs',
                class: 'mt-[2px] focus:outline-none text-[15px]',
              })
            @end
          </div>
          <div class="p-2 w-1/4 flex">
            <button
              class="bg-amber-500 text-neutral-900 w-full h-full rounded-xl hover:bg-amber-500/80 cursor-pointer font-semibold"
            >
              Rechercher
            </button>
          </div>
        @end

        <div class="flex gap-3 my-6">
          <span
            class="text-[13px] leading-[1.20] text-white border border-white/55 px-3 py-2 rounded-full"
          >
            Week-end
          </span>
          <span
            class="text-[13px] leading-[1.20] text-white border border-white/55 px-3 py-2 rounded-full"
          >
            Permis B
          </span>
          <span
            class="text-[13px] leading-[1.20] text-white border border-white/55 px-3 py-2 rounded-full"
          >
            4 couchages
          </span>
          <span
            class="text-[13px] leading-[1.20] text-white border border-white/55 px-3 py-2 rounded-full"
          >
            Animaux acceptés
          </span>
        </div>
      </div>
    </div>
  </header>
  <div class="border-b border-line">
    <div
      class="container mx-auto px-4 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-0 py-6 lg:py-0 lg:divide-x lg:divide-line"
    >
      <div class="lg:p-6 lg:first:pl-0">
        <h3 class="font-semibold text-ink">Assurance incluse</h3>
        <p class="text-muted text-sm">Tous risques pendant toute la durée du séjour</p>
      </div>
      <div class="lg:p-6">
        <h3 class="font-semibold text-ink">Annulation 48 h</h3>
        <p class="text-muted text-sm">Remboursement intégral sans justificatif</p>
      </div>
      <div class="lg:p-6">
        <h3 class="font-semibold text-ink">Assistance 24/7</h3>
        <p class="text-muted text-sm">Dépannage partout en Belgique et en Europe</p>
      </div>
      <div class="lg:p-6">
        <h3 class="font-semibold text-ink">Profils vérifiés</h3>
        <p class="text-muted text-sm">Identité et permis contrôlés des deux côtés</p>
      </div>
    </div>
  </div>

  <main class="container mx-auto px-4">
    <section class="mt-10">
      <h3 class="font-bold text-ink text-2xl">Par type de véhicule</h3>
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4 mt-4">
        @each(type in types)
          {{-- Lien vers le catalogue complet : le filtre par type réel arrive avec F4 --}}
          @link({
            route: 'vehicles.index',
            class:
              'block bg-white border border-line rounded-2xl overflow-hidden hover:shadow-lg transition-shadow',
          })
            <div
              class="h-24 bg-amber-100 flex items-center justify-center"
              style="background:{{ type.tint }}"
            >
              <i class="fa-solid {{ type.icon }} text-3xl text-ink/70"></i>
            </div>
            <div class="p-3 5">
              <p class="font-semibold text-ink">{{ type.name }}</p>
              <p class="text-sm muted mt-1">
                <span>
                  {{ type.vehiclesCount }}
                  annonce{{ type.vehiclesCount === 1 ? '' : 's' }}
                </span>
                @if(type.minPrice)
                  - dès
                  {{ type.minPrice }}
                  € / nuit
                @end
              </p>
            </div>
          @end
        @end
      </div>
    </section>

    <section class="mt-14">
      <div class="flex items-end justify-between gap-4 flex-wrap">
        <div>
          <h3 class="font-bold text-ink text-2xl">Les mieux notés</h3>
          <p class="text-muted text-sm mt-1">Sélection des dernières annonces publiées</p>
        </div>
        @link({
          route: 'vehicles.index',
          class:
            'text-sm font-semibold text-ink border border-ink rounded-lg px-3.5 py-2.5 hover:bg-ink hover:text-white transition-colors',
        })
          Voir les
          {{ vehiclesTotal }}
          véhicules
        @end
      </div>
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-4">
        @each(item in vehicles)
          @link({
            route: 'vehicles.show',
            routeParams: [item.vehicle.id],
            class:
              'block border border-line rounded-2xl overflow-hidden bg-white hover:shadow-lg transition-shadow',
          })
            <div class="relative aspect-4/3">
              <img
                class="w-full h-full object-cover"
                src="{{ (await driveUrl(item.vehicle.pictures[0]?.path)) || '/images/default.webp' }}"
                alt=""
              />
              <span
                class="absolute top-3 left-3 bg-ink/85 text-white text-xs font-semibold px-2.5 py-1 rounded-lg"
              >
                {{ item.vehicle.type.name }}
              </span>
            </div>
            <div class="p-4 space-y-1.5">
              <div class="flex items-start justify-between gap-3">
                <p class="font-semibold text-neutral-900 leading-tight">{{ item.vehicle.model }}</p>
                <p class="text-sm text-neutral-800 whitespace-nowrap">
                  <i class="fa-solid fa-star text-accent"></i>
                  {{ item.rating }}
                  <span class="text-[#666D7D]">({{ item.reviewsCount }})</span>
                </p>
              </div>
              <p class="text-sm text-muted">
                {{ item.vehicle.beds }}
                couchages -
                {{ item.vehicle.seats }}
                places
              </p>
              <p class="text-sm text-muted">{{ item.vehicle.city }}</p>
              <div class="flex items-end justify-between pt-2 border-t border-neutral-100 mt-2">
                <p>
                  <span class="text-lg font-bold text-ink">{{ item.vehicle.price }} €</span>
                  <span class="text-sm text-muted">/ nuit</span>
                </p>
              </div>
            </div>
          @end
        @else
          <p class="text-muted col-span-3">Aucune annonce publiée pour le moment.</p>
        @end
      </div>
    </section>

    <section class="mt-14">
      <h3 class="font-bold text-ink text-2xl">Comment ça marche</h3>
      <div class="grid sm:grid-cols-3 gap-6 mt-4">
        <div class="border-t-2 border-accent pt-3.5">
          <p class="text-xs font-bold text-accent-dark uppercase tracking-wide">Étape 1</p>
          <p class="font-semibold text-ink mt-1 5">Cherchez et comparez</p>
          <p class="text-sm text-muted mt-1 leading-relaxed">
            Filtrez par ville, dates et couchages, puis comparez les prix par nuit.
          </p>
        </div>
        <div class="border-t-2 border-accent pt-3.5">
          <p class="text-xs font-bold text-accent-dark uppercase tracking-wide">Étape 2</p>
          <p class="font-semibold text-ink mt-1 5">Réservez vos dates</p>
          <p class="text-sm text-muted mt-1 leading-relaxed">
            La disponibilité du véhicule est vérifiée en direct sur les dates choisies.
          </p>
        </div>
        <div class="border-t-2 border-accent pt-3.5">
          <p class="text-xs font-bold text-accent-dark uppercase tracking-wide">Étape 3</p>
          <p class="font-semibold text-neutral-900 mt-1 5">Prenez la route</p>
          <p class="text-sm text-muted mt-1 leading-relaxed">
            Remise des clés en main propre avec le propriétaire, le jour du départ.
          </p>
        </div>
      </div>
    </section>

    <section
      class="mt-14 mb-16 bg-[#FDF0C9] border border-[#F0DFA8] rounded-2xl p-6 sm:p-8 flex flex-col sm:flex-row items-center gap-6"
    >
      <div class="flex-1">
        <h3 class="font-bold text-ink text-2xl">Vous avez un camping-car ? Rentabilisez-le.</h3>
        <p class="text-muted-dark mt-2 max-w-[60ch]">
          Déposez votre annonce gratuitement et fixez votre propre prix par nuit.
        </p>
      </div>
      @link({
        class:
          'shrink-0 bg-accent text-ink font-semibold px-5 py-3.5 rounded-xl hover:bg-accent/80 transition-colors',
      })
        Mettre mon véhicule en location
      @end
    </section>
  </main>

  <footer class="bg-ink">
    <div class="container mx-auto px-4 py-10 grid sm:grid-cols-2 lg:grid-cols-4 gap-8">
      <div>
        <div class="text-xl uppercase text-white border-y-2 border-accent py-1 inline-block">
          Loca<span class="text-accent">van</span>
        </div>
        <p class="text-white/70 text-sm mt-3 max-w-[34ch]">
          La location de camping-cars entre particuliers, sans intermédiaire.
        </p>
      </div>
      <div>
        <p class="text-accent text-xs font-semibold uppercase tracking-wide">Louer</p>
        <div class="flex flex-col gap-2 mt-3 text-sm text-white/80">
          @link({ route: 'vehicles.index', class: 'hover:text-white' })
            Tous les véhicules
          @end

          <a href="#" class="hover:text-white">Idées d'itinéraires</a>
          <a href="#" class="hover:text-white">Prix et frais</a>
        </div>
      </div>
      <div>
        <p class="text-accent text-xs font-semibold uppercase tracking-wide">Loueurs</p>
        <div class="flex flex-col gap-2 mt-3 text-sm text-white/80">
          @link({ route: 'vehicles.create', class: 'hover:text-white' })
            Mettre son van en location
          @end

          <a href="#" class="hover:text-white">Assurance propriétaire</a>
          <a href="#" class="hover:text-white">Guide du loueur</a>
        </div>
      </div>
      <div>
        <p class="text-accent text-xs font-semibold uppercase tracking-wide">Locavan</p>
        <div class="flex flex-col gap-2 mt-3 text-sm text-white/80">
          <a href="#" class="hover:text-white">Aide</a>
          <a href="#" class="hover:text-white">Nous contacter</a>
          <a href="#" class="hover:text-white">CGU</a>
          <a href="#" class="hover:text-white">Confidentialité</a>
        </div>
      </div>
    </div>
  </footer>
@end
